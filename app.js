// Kompatibilität: Die frühere große `app.js` wurde in mehrere Dateien unter `kursteam/` aufgeteilt
// und wird direkt in `ms365-schooltool.html` eingebunden.
//
// Diese Datei bleibt als "leere" Kompatibilitätsschicht erhalten, falls irgendwo noch `app.js`
// referenziert wird (z. B. alte Bookmarks/Deployments).
(function () {
    'use strict';
    // no-op
})();

